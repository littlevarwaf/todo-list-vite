// src/types.ts

// TODO: Lab Step 2 - สร้าง Interface สำหรับ Todo
// กำหนดให้มี id (number), title (string), และ completed (boolean)
export interface Todo {
    id: number;
    title: string;
    completed: boolean;
}